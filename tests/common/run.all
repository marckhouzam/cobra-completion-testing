#!sh
# This script runs for all shells bash, zsh, fish, so it's syntax must stay
# compatible with all shells

##############################################
# Run all tests for the specified $SHELL_TYPE
##############################################

cd $TESTING_DIR

# Source the testing logic
source $TESTS_DIR/$SHELL_TYPE/comp-test-lib.$SHELL_TYPE

# Run the shell specific tests
source $TESTS_DIR/$SHELL_TYPE/comp-tests.$SHELL_TYPE
